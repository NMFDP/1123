//task 1
import React from "react";
import { View, Text, StyleSheet } from "react-native";

export default function App() {
  return (
    <View style={styles.container}>
      <View style={styles.box}>
        <Text style={styles.text}>Naan Mudhalvan Session</Text>
      </View>
      <View style={styles.box}>
        <Text style={styles.text}>Welcome to React Native!</Text> {/* New Box Added */}
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  box: {
    width: "80%",
    height: 100,
    backgroundColor: "tomato",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 10,
    marginVertical: 10, // Space between boxes
  },
  text: {
    color: "white",
    fontSize: 18,
    fontWeight: "bold",
  },
});

